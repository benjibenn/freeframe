/**
 * Title used wherever a name is structurally required and none is available —
 * the browser tab when no page title is set, and the app's default metadata.
 * Deliberately descriptive rather than a brand: this build ships unbranded and
 * each tenant supplies its own name via Settings → Branding.
 */
export const APP_TITLE = 'Media Review'
